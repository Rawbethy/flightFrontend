import {useState, useEffect, useContext} from 'react';
import {useNavigate} from 'react-router-dom';

export default function Recents() {

    return (
        <div style={{color: 'white', fontSize: '50px'}}>This is the Recents component</div>
    )
}